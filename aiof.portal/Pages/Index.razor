@page "/"

@inject ICookieStorageService  _cookieStorageService

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

@code {
    protected override async Task OnInitializedAsync()
    {
        await _cookieStorageService.SetAsync<User>("test", DateTime.UtcNow.AddDays(7));
    }
}
